import java.util.Scanner;
public class Cube_Conundrum {
    Scanner scan = new Scanner("input.txt");
    // I need to go line by line and then delimit the String by semicolons
    // I can identify which color easily by seeing in the first letter is either r,g or b
    // I need to map the numbers stored by each number in a map
    // I then can see if the individual totals are possible for each substring
    //if so then add the id to a running sum
    //if not then skip
    //return sum

    public 
}